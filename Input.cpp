#pragma once

#include <GL/glew.h>
#include <GLFW/glfw3.h>

#include <iostream>

#include "Input.h"

/*void Input::KeyBorad::HandleKey(GLFWwindow* window, int key, int scancode, int action, int mods)
{

	Input::KeyBorad& handler = *reinterpret_cast<Input::KeyBorad*>(glfwGetWindowUserPointer(window));
	handler.handleKeyImpl(window, key, scancode, action, mods);
	
}*/

/*void Input::KeyBorad::handleKeyImpl(GLFWwindow* window, int key, int scancode, int action, int mods)
{
	if (key == GLFW_KEY_F && action == GLFW_PRESS)
	{
		std::cout << key << " pressed.";
	}
}*/
